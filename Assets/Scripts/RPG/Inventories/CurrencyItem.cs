using UnityEngine;

namespace RPG.Inventories
{
  /// <summary>
  /// Money
  /// used to create money pickups
  /// </summary>
  [CreateAssetMenu(fileName = "New Currency Item", menuName = "InventorySystem/New Currency Item", order = 0)]
  public class CurrencyItem : InventoryItem
  {
  }
}